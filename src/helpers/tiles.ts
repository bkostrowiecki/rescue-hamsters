export enum PlayerTiles {
    NONE = -1,
    TOP_GROUND = 0,
    MIDDLE_GROUND = 1,
    SPRING = 2
}

export enum PredefinedTiles {
    NONE = -1,
    START_POINT = 0,
    FINISH_POINT = 1,
    IVY_MIDDLE = 2,
    IVY_BOTTOM = 3
}